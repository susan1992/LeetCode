class Solution {
public:
    string reverseString(string s) {
        char temp;
        int len = s.length()-1;
        for(int i=0; i<=len/2; i++){
            temp = s[i];
            s[i] = s[len-i];
            s[len-i] = temp;
        }
     return s;     
    }
};